default:
  DEBUG: true
  NAME: backend
  HOST: localhost
  PORT: 8080
  WORKERS: 4
  FAST_API_PATH: backend.app:app
  RELOADED: false
  LOG_LEVEL: info


  DATABASE:
    DRIVER: postgresql
    HOST: localhost
    PORT: 5732
    USER: postgres
    PASSWORD: postgres
    NAME: qme_back

  LOGGING:
